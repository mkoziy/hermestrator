# Plan: Hermes coding-agent Docker image

Собрать Docker-образ кодинг-агента на базе Hermes Agent (nousresearch/hermes-agent), с git/gh/fzf/jq/ripgrep, Go-тулчейном, Node.js, codex CLI, pi CLI и ralphex-бинарником внутри; скопировать в образ ralphex-профили (codex/pi/claude) из `ralphex/`; настроить идемпотентный entrypoint и self-backup в приватный GitHub-репозиторий через встроенный cron Hermes.

k3s-деплой (StatefulSet/Deployment, manifests, Secret) вынесен из этой итерации — пока только образ и локальная сборка/проверка. Вернуться к деплою отдельным планом позже.

**(2026-07-20) Self-backup выведен из этой итерации.** Task 7 (`hermes-backup.sh`,
регистрация cron-задачи бэкапа, restore-from-backup ветка в `entrypoint.sh`) был
реализован и затем полностью удалён по явному запросу — self-backup будет
сделан отдельным планом позже, аналогично k3s-деплою. Чекбоксы Task 7 ниже и
соответствующие пункты Task 6/9 оставлены как исторический след реализации,
но код, на который они ссылаются, в текущем дереве больше не существует — не
ориентироваться на них как на актуальное состояние `docker/`.

Все архитектурные решения (base image, non-root user, entrypoint idempotency rules, backup flow, env-var-only secrets) уже приняты в брейнсторм-сессии — не переоткрывать их. Версии (Go, Node LTS, ralphex release) проверять заново на момент выполнения задачи, не брать как константы из этого файла.

**Предположения (подтверждены по факту реализации — не TODO, оба значения финализированы и используются по всему деплою; при необходимости другого значения — задать через `--build-arg`/env, см. README):**
- Имя образа: `ghcr.io/mkoziy/hermes-coding-agent` (registry — GHCR, т.к. `gh`/GitHub уже в центре стека). Зафиксировано в README (build/push-секции).
- Расписание cron-бэкапа: ежедневно в 03:00 UTC (`0 3 * * *`, дефолт `HERMES_BACKUP_CRON_SCHEDULE` в `entrypoint.sh`), плюс ручной триггер (`hermes-backup.sh` напрямую или `hermes cron run hermes-home-backup`, задокументировано в README).

**Подтверждено сверкой с реальным репозиторием/докой Hermes (`github.com/NousResearch/hermes-agent`, `hermes-agent.nousresearch.com/docs`) на 2026-07-19 — не перепроверять существование этих команд/страниц заново, но версии/синтаксис флагов сверять по месту:**
- `hermes config set|get`, `hermes doctor`, `hermes cron add|edit|runs`, `hermes gateway install|setup|start|status|stop` — реальные подкоманды, подтверждены в README и `docs/reference/cli-commands`.
- У Hermes два независимых входа: `hermes` — интерактивный TUI; `hermes gateway` — headless-процесс для messaging-платформ (Telegram/Discord/Slack/WhatsApp/Signal/Email) + встроенный cron. Для этого деплоя (доступ только через messaging/gateway) PID 1 контейнера почти наверняка должен быть `hermes gateway`, не голый `hermes` — see Task 6.
- Есть официальная страница `docs/user-guide/security` с разделами "Dangerous Command Approval" (approval modes, YOLO mode), "DM Pairing System", "Container Isolation", "Terminal Backend Security Comparison", "Environment Variable Passthrough" — это готовый гайд именно под этот сценарий (Hermes в контейнере, headless), прочитать целиком перед Task 6, не изобретать заново.
- Hermes сам умеет запускать команды через отдельный "Docker" terminal backend (один из шести: local/Docker/SSH/Singularity/Modal/Daytona) — это Hermes, спавнящий контейнеры для sandboxing инструментов, ОТДЕЛЬНО от того, что сам Hermes работает в нашем контейнере. Если backend по умолчанию — `docker`, а `docker.sock`/DinD в контейнере нет, тулинг сломается на первом вызове инструмента. Явно выставить terminal backend в `local`.
- Установщик управляет собственным Node.js/Python 3.11 через `uv`, если системные версии отсутствуют/устарели (переписывает npm global prefix под себя). Дублирующая установка Node/Python в Task 3 — реальный риск коллизии PATH/npm-prefix, не только гипотетический.

## Validation Commands
- `docker build -f docker/Dockerfile -t hermes-coding-agent:local .` (Dockerfile lives under `docker/`, build context is the repo root — see Task 9/README)
- `docker run --rm hermes-coding-agent:local hermes doctor` (ожидаем осмысленный вывод диагностики, не краш)
- `docker run --rm hermes-coding-agent:local bash -lc 'go version && node --version && python3 --version && ralphex --version && codex --version && pi --version && gh --version && git --version && fzf --version && jq --version'`
- `docker run --rm -i hadolint/hadolint < docker/Dockerfile` (нет локального бинаря `hadolint` — используется его официальный Docker-образ, как и в Task 9)

### Task 1: Инициализировать git-репозиторий и базовую структуру
- [x] Репозиторий `/Users/michael/github.com/mkoziy/coding` сейчас не git-репозиторий — выполнить `git init`, создать `.gitignore` (как минимум: `.env`, `*.env`, `**/*secret*`, `**/credentials*`, `progress/`, `worktrees/` — последние два уже есть в `ralphex/ralphex-pi/.gitignore`, унифицировать на верхнем уровне)
- [x] Создать `docker/` каталог для Docker-артефактов (или разместить в корне — решить по месту, главное консистентно с остальным репо)
- [x] Сделать первый коммит с текущим состоянием (`ralphex/` конфиги как есть)

### Task 2: Собрать Dockerfile — базовый слой и системные пакеты
- [x] `FROM debian:bookworm-slim`, `ARG GO_VERSION`, `ARG NODE_MAJOR` как build args с дефолтами (значения подставить актуальные на момент сборки — проверить `curl -s https://go.dev/VERSION?m=text` для Go и текущий Node LTS major)
- [x] Создать непривилегированного пользователя `app` (`useradd -m -s /bin/bash app`), `HOME=/home/app`
- [x] Установить system packages: `git curl ca-certificates jq ripgrep build-essential ffmpeg unzip fzf` через `apt-get install --no-install-recommends`, почистить apt-кэш в том же слое
- [x] Установить `gh` CLI через официальный apt-репозиторий (`cli.github.com`)

### Task 3: Установить Go, Node.js, Python/uv тулчейны
- [x] Скачать и распаковать Go tarball (`https://go.dev/dl/go${GO_VERSION}.linux-<arch>.tar.gz`) в `/usr/local/go`, добавить `/usr/local/go/bin` в `PATH`, задать `GOPATH=/home/app/go` для пользователя `app`
- [x] Установить Node.js LTS через NodeSource setup-скрипт
- [x] Установить `uv` (astral) явно (`curl -LsSf https://astral.sh/uv/install.sh | sh`), Python 3.11 через uv или system python3.11 — сверено с реальным install.sh: Hermes ВСЕГДА ставит собственный Python 3.11 через `uv python install`/`uv venv`, независимо от системного python3 — конфликта с system python3 нет (в отличие от Node). System `python3`/`python3-venv` установлены через apt для общей доступности и валидационных команд плана
- [x] Проверить, что все три рантайма доступны в `PATH` для пользователя `app` (не только root) — `PATH`/`GOPATH` заданы через `ENV` (действуют для всех пользователей образа), `/home/app/go` создан и `chown`-нут на `app`
- [x] Подтверждённый риск (не гипотетический): Hermes installer сам ставит управляемый Node.js в `$HERMES_HOME/node` и переписывает npm global prefix (`$HERMES_HOME/node/etc/npmrc`), если найденный системный Node не проходит `node_satisfies_build` (`^20.19 || >=22.12`). Решено: системный Node ставится версией NODE_MAJOR=24 (>=22.12) через NodeSource — сверено напрямую по коду install.sh (`node_satisfies_build`), installer обнаружит этот Node как достаточный и не будет трогать Node вообще

### Task 4: Установить Hermes Agent, Mnemosyne, codex, pi, ralphex
- [x] Переключиться на `USER app`, `WORKDIR /home/app`
- [x] Установить Hermes: `curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash` — НЕ вызывать `hermes setup` на этом шаге (интерактивный, требует ключи); используются `--skip-setup --non-interactive`
- [x] `pip install "mnemosyne-memory[all]"` (или через uv) — установлено через `uv tool install "mnemosyne-memory[mcp,embeddings,openclaw]"`, сознательно БЕЗ буквального `[all]`: этот extra тянет `llm` (ctransformers+llama-cpp-python), которому нет прекомпилированного wheel здесь и нужен `cmake` (не установлен) для сборки из исходников — локальный LLM-инференс не нужен в этом деплое (Hermes ходит к LLM по API), причина задокументирована в Dockerfile
- [x] `npm install -g` для codex CLI и pi CLI — имена пакетов сверены по факту через `npm view`: `@openai/codex` (bin `codex`) и `@earendil-works/pi-coding-agent` (bin `pi`, описание пакета совпадает с ожиданиями `ralphex-pi/scripts/pi-as-claude.sh`)
- [x] Скачать последний релиз `umputun/ralphex` под нужную архитектуру с GitHub Releases API, положить бинарник в `/usr/local/bin/ralphex`, `chmod +x`
- [x] Проверить версии всех пяти инструментов в самом образе (см. Validation Commands) — все проверены полным `docker build` + `docker run`; попутно найдены и исправлены три реальных бага: (1) `ENV HOME=/home/app`, выставленный сразу после `useradd`, заставлял ВСЕ последующие root-шаги (включая установщик `uv`) писать root-owned файлы в `/home/app` — перенесено на момент прямо перед `USER app`; (2) `UV_INSTALL_DIR=/usr/local/bin curl ... | sh` — переменная окружения применялась только к `curl`, а не к `sh`, читающему её из пайпа — исправлено через `export`; (3) Debian `/etc/profile` безусловно перезаписывает `PATH` в login-shell по uid, из-за чего login shell (`bash -l`, `docker exec -it ... bash --login`) терял весь наш PATH (Go/Node/npm-global/uv/.local) — добавлен `/etc/profile.d/00-hermes-path.sh`, восстанавливающий PATH после этого сброса

### Task 5: Скопировать ralphex-профили и написать переключатель профилей
- [x] `COPY --chown=app:app ralphex/ralphex-codex/config /opt/ralphex-profiles/codex/config`
- [x] `COPY --chown=app:app ralphex/ralphex-pi/{config,agents,prompts,scripts} /opt/ralphex-profiles/pi/` (сохранить внутреннюю структуру подпапок) — реализовано как 4 отдельные `COPY` (Dockerfile `COPY` не поддерживает brace-expansion), структура подпапок сохранена
- [x] `COPY --chown=app:app ralphex/ralphex-claude/config /opt/ralphex-profiles/claude/config`
- [x] `chmod +x` на все `.sh`-скрипты внутри скопированных `scripts/` (`pi-as-claude.sh`, `pi-opencode-go.sh` и любые другие)
- [x] Написать `/usr/local/bin/ralphex-use-profile.sh`: принимает один аргумент (`codex|pi|claude`), валидирует его, делает `rm -rf ~/.config/ralphex && cp -r /opt/ralphex-profiles/$1/* ~/.config/ralphex/` (или symlink — решить, что проще поддерживать; symlink избегает копирования, но COPY-слой всё равно read-only для образа, так что копирование в volume проще при первом старте) — реализовано копированием (`cp -r "$src/." "$dest/"`), как и предполагает формулировка чекбокса

### Task 6: Написать entrypoint.sh (идемпотентный)
- [x] **Перед написанием entrypoint — прочитать целиком `docs/user-guide/security` (Dangerous Command Approval / YOLO Mode / DM Pairing System / Container Isolation / Terminal Backend Security Comparison / Environment Variable Passthrough) и `docs/user-guide/messaging`** на `hermes-agent.nousresearch.com/docs` — эти страницы прямо описывают целевой сценарий (headless Hermes в контейнере), не изобретать решения ниже вслепую — прочитано через WebFetch (плюс `docs/user-guide/configuration` и `docs/reference/cli-commands` дополнительно, для сверки точных имён ключей/флагов)
- [x] Проверка первого запуска: `[ -d "$HERMES_HOME" ] && [ -n "$(ls -A "$HERMES_HOME" 2>/dev/null)" ]` — если volume пуст И задан `HERMES_BACKUP_REPO`, клонировать бэкап-репозиторий в `$HERMES_HOME` (восстановление состояния); если пуст и репозиторий не задан/не существует — обычная первая инициализация
- [x] Настроить git identity из env (`GIT_AUTHOR_NAME`, `GIT_AUTHOR_EMAIL` или `GIT_USER_NAME`/`GIT_USER_EMAIL` — выбрать единый набор имён переменных и задокументировать в README) — выбран `GIT_USER_NAME`/`GIT_USER_EMAIL`, маппится на `git config --global user.name/user.email`; обоснование в комментарии entrypoint.sh, README-запись — Task 9
- [x] `gh auth login` через `GH_TOKEN` (неинтерактивно, `gh auth login --with-token <<< "$GH_TOKEN"` или `echo $GH_TOKEN | gh auth login --with-token`)
- [x] Неинтерактивная конфигурация провайдера Hermes из env: для каждого нужного `hermes config set <key> <value>` — сначала `hermes config get <key>`, ставить только если отличается (idempotent write). Команда подтверждена реальной (см. шапку плана)
- [x] Явно решить и задать режим одобрения опасных команд для headless-запуска (без TTY команды подтверждать некому) — либо `HERMES_YOLO_MODE=1` (полный автопропуск), либо настроить DM Pairing System так, чтобы одобрения приходили пользователю в чат мессенджера; задокументировать выбор и его риски в README (Task 9), не оставлять как implicit default — решено: approvals остаются включены (`approvals.mode: smart` по умолчанию) + DM Pairing (`unauthorized_dm_behavior: pair`), одобрения приходят пользователю в чат ответом yes/no (документированный механизм гейтвея); `HERMES_YOLO_MODE=1` оставлен как явный opt-in для оператора, по умолчанию выключен
- [x] Явно выставить terminal backend в `local` (env var/`hermes config set`, сверить точное имя ключа по `docs/user-guide/security#terminal-backend-security-comparison`) — без этого возможен неявный fallback на `docker`-backend, которому нужен `docker.sock`/DinD, не предусмотренный в этом поде — ключ подтверждён: `terminal.backend`, значения `local|docker|ssh|singularity|modal|daytona`
- [x] Рассмотреть `HERMES_GATEWAY_NO_SUPERVISE` (или актуальный эквивалент): у `hermes gateway` есть собственный internal restart-loop (`HERMES_GATEWAY_MAX_STARTS`), который может конфликтовать с внешним supervisor'ом контейнера (`docker restart`, будущий k8s restart policy) — решить, кто супервайзит рестарты, не оставлять оба уровня активными без проверки — подтверждено по докам: `HERMES_GATEWAY_NO_SUPERVISE=1` эквивалентен флагу `--no-supervise` (`hermes gateway run`); `HERMES_GATEWAY_MAX_STARTS` в доступной документации не найден. Решено: `HERMES_GATEWAY_NO_SUPERVISE=1` выставляется безусловно (если не переопределён) — единственный уровень супервизии рестартов — внешний (docker/k8s)
- [x] Вызвать `ralphex-use-profile.sh "${RALPHEX_DEFAULT_PROFILE:-claude}"`
- [x] НЕ трогать/не удалять `*.db-wal` / `*.db-shm` файлы mnemosyne при старте
- [x] Зарегистрировать/убедиться, что cron-задача бэкапа существует в Hermes (см. Task 7) — идемпотентно, через `hermes cron` (list/get подкоманды подтверждены реальными, см. шапку плана) — реализовано против подтверждённого синтаксиса `hermes cron create`/`hermes cron list`; сам `hermes-backup.sh` и точный синтаксис schedule-строки — предмет Task 7, регистрация non-fatal на случай если скрипт ещё не существует
- [x] Финально: **`exec hermes gateway` (не голый `hermes`)** — `hermes` без аргументов запускает интерактивный TUI, а доступ к этому деплою предполагается только через messaging-платформы/gateway (см. шапку плана); foreground, PID 1, без supervisord — если по факту окажется не так (например, нужен `hermes` + отдельно поднятый gateway), задокументировать почему и скорректировать — подтверждено по `docs/user-guide/messaging`: голый `hermes gateway` = foreground run, эквивалент `hermes gateway run`; отклонений не потребовалось

### Task 7: Написать hermes-backup.sh и настроить cron внутри Hermes
- [x] Скрипт `hermes-backup.sh`: `cd "$HERMES_HOME" && git add -A && git diff --cached --quiet || git commit -m "backup $(date -u +%FT%TZ)"`, затем `git push origin <branch>` (без `--force`) — реализовано в `docker/hermes-backup.sh`; дополнительно самолечится на самом первом запуске (`git init -b main` + `git remote add origin` из `HERMES_BACKUP_REPO`, если `$HERMES_HOME` ещё не git-репозиторий/без remote), иначе — `log` + `exit 1`, без падения вызывающего процесса
- [x] Создать/проверить `.gitignore` внутри `$HERMES_HOME` (генерируется при первой инициализации, если отсутствует): исключить `**/*secret*`, `**/credentials*`, `.env`, любые файлы с токенами, которые Hermes мог бы туда положить — реализовано как `ensure_gitignore()` внутри `hermes-backup.sh` (create-if-missing на каждом запуске, а не в `entrypoint.sh`, чтобы гарантированно существовать перед первым `git add -A` независимо от пути инициализации); список сверен по `docs/reference/cli-commands` на момент выполнения: `.env`, `auth-profiles.json`, `pairing/`, `webhook_subscriptions.json`, плюс generic `*secret*`/`*credentials*` и `*.db-wal`/`*.db-shm` (WAL-сайдкары, чтобы не закоммитить рваный снапшот)
- [x] Настроить cron-задачу через `hermes cron add`/`create` на ежедневный запуск `hermes-backup.sh` (расписание — ежедневно 03:00 UTC, подтверждено как решение из шапки плана, т.к. в автономном режиме нет пользователя для подтверждения) — точный синтаксис сверен через `docs/user-guide/features/cron`: script-only задачи используют `--no-agent --script <path>` (без позиционного prompt-аргумента) и cron-expression расписания вида `"0 3 * * *"`; `entrypoint.sh`'s `ensure_backup_cron_registered()` (заглушка из Task 6) исправлена под этот подтверждённый синтаксис, идемпотентность через `hermes cron list | grep -qF "$job_name"` сохранена
- [x] Задокументировать, как вручную запустить бэкап (на случай, если пользователь попросит "забэкапься сейчас") — комментарий-хедер в `docker/hermes-backup.sh`: `docker exec <container> /usr/local/bin/hermes-backup.sh`, либо через сам Hermes cron: `docker exec <container> hermes cron run hermes-home-backup`

### Task 8: .dockerignore и верхнеуровневый README
- [x] `.dockerignore`: `.git`, `docs/plans/completed`, локальные `.env` — реализовано в `/.dockerignore`; дополнительно исключены `.gitignore` (VCS-метаданные) и `.DS_Store`/`**/.DS_Store` (OS cruft), паттерны `.env`/`*.env` намеренно не задевают `.env.example` (проверено вручную: `*.env` требует, чтобы имя файла оканчивалось буквально на `.env`, `.env.example` оканчивается на `.example`)
- [x] `README.md`: как собрать образ локально, как пушить в GHCR, как запустить контейнер локально (`docker run` с `.env`), список обязательных env-переменных с описанием (включая `HERMES_YOLO_MODE`/approval-режим, terminal backend, `HERMES_GATEWAY_NO_SUPERVISE` из Task 6), как переключить ralphex-профиль вручную, как посмотреть логи бэкапа, известные ограничения. Явно отметить, что деплой в k3s — предмет отдельного плана, не этого — реализовано в `/README.md`, весь список env-переменных вычитан напрямую из `docker/entrypoint.sh`/`docker/hermes-backup.sh`/`docker/Dockerfile` (не придуман), включая дефолты (`approvals.mode=smart`, `terminal.backend=local`, `HERMES_GATEWAY_NO_SUPERVISE=1`, `RALPHEX_DEFAULT_PROFILE=claude`, cron `0 3 * * *`); раздел "Known limitations" явно выносит k3s-деплой в отдельный будущий план

### Task 9: Локальная сборка и сквозная проверка
- [x] `docker build` образа, прогнать все Validation Commands из шапки плана — все прошли (`docker build`, `hermes doctor`, полный tool-version bash-oneliner, `hadolint` через `docker run hadolint/hadolint` — локального бинаря нет, но Docker-образ доступен и был использован; только warnings, 0 errors). По пути найден и исправлен реальный баг: `docker run <image> hermes doctor` / `... bash -lc '...'` (буквально Validation Commands из шапки плана) раньше падали, потому что `entrypoint.sh` безусловно делал `exec hermes gateway "$@"`, склеивая переданную команду с `hermes gateway` вместо того чтобы выполнить её саму по себе — стандартный ENTRYPOINT+CMD паттерн ("если переданы аргументы — exec их напрямую, иначе — gateway по умолчанию") восстановлен
- [x] Запустить контейнер локально с тестовым `.env` (использованы фиктивные/тестовые значения — `GH_TOKEN` пустой, `ANTHROPIC_API_KEY=sk-test-fake-...`, локальный bare git-репозиторий вместо реального GitHub — реальные креды не использовались), проверить: `hermes doctor` зелёный (осмысленный вывод, exit 0; предупреждения про невалидный fake-ключ и незаданные мессенджер-токены ожидаемы и не являются падением), `ralphex-use-profile.sh pi && cat ~/.config/ralphex/config` показывает pi-профиль (подтверждено), `hermes-backup.sh` создаёт коммит в тестовом bare-репозитории (подтверждено — коммит и push прошли, `.gitignore` корректно исключил лишнее). По пути найдены и исправлены ДВА реальных бага, обнаруженных именно благодаря запуску с настоящим volume-mount (не воспроизводились при чистой `docker build`/`docker run` без volume в Task 4/8): (1) установщик Hermes кладёт собственное приложение (`hermes-agent/` — ~1.6GB venv+node_modules+исходники, и `bin/` — приватные uv/uvx) ВНУТРЬ `$HERMES_HOME`, той же директории, что этот план трактует как чистый persistent-state volume — монтирование любого volume поверх (включая пустой первый запуск и restore-from-backup) стирало установленное приложение и `hermes` переставал запускаться; исправлено добавлением seed-копии (`$HOME/.hermes-seed`, бэкается в Dockerfile) и идемпотентным reseed-шагом в `entrypoint.sh` (докопирует только если отсутствует, не трогает существующее); (2) `hermes cron create --script <путь>` из Task 7 никогда реально не регистрировал cron-задачу — реальный бинарь `hermes` требует, чтобы скрипт физически лежал внутри `~/.hermes/scripts/` и передавался только именем файла (абсолютный путь и даже symlink наружу отклоняются с ошибками "must be relative"/"escapes the scripts directory via traversal") — исправлено копированием (не symlink) `hermes-backup.sh` в `$HERMES_HOME/scripts/` на каждом старте перед регистрацией; также `hermes-backup.sh`'s `.gitignore` дополнен исключениями `hermes-agent/`/`bin/`, чтобы бэкап не пытался закоммитить гигабайты установленного приложения; (3) невалидный/фиктивный `GH_TOKEN` (именно тот сценарий, который эта задача предписывает использовать вместо реальных кредов) полностью ронял весь контейнер на старте — `gh auth login --with-token` возвращал ненулевой код, а `set -euo pipefail` прерывал entrypoint целиком до вызова `exec hermes gateway`; исправлено оборачиванием вызова в `if ! ...; then warn ...; fi`, как уже сделано для отсутствующей git identity — теперь при плохом токене выводится WARNING и инициализация продолжается (подтверждено: `docker run` с заведомо невалидным `GH_TOKEN` теперь доходит до конца entrypoint с exit 0 вместо падения)
- [x] Замерить время холодного старта — на пустом/только что восстановленном из бэкапа volume: ~46.7s от `docker run` до лога `entrypoint: starting hermes gateway` (доминирует одноразовый reseed ~1.6GB приложения через `cp -a`); сам процесс `hermes gateway` после этого поднимается и стабильно висит "Up" (WARNING про отсутствие messaging-платформ — ожидаемо без реальных токенов, не падение). Число пригодится как отправная точка для будущих readiness/liveness проб, калибровку по факту стоит повторить на целевом железе/сети (не на локальном macOS Docker Desktop)
- [x] Прогнать restart-сценарий: `docker kill` контейнера с populated volume, повторный `docker run` с тем же volume — entrypoint не упал, корректно определил "$HERMES_HOME already populated, skipping restore/init", НЕ переделал reseed (hermes-agent/bin уже на месте), НЕ задублировал cron-задачу ("already registered, skipping" — тот же job id `15abb8e64f0d` до и после). Время до `starting hermes gateway` на restart: ~1.2s (vs ~46.7s холодного старта) — подтверждает, что дорогая часть инициализации происходит один раз, а не на каждый рестарт
